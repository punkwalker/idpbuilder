apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: aws-agent
  namespace: kube-system
spec:
  template:
    spec:
      containers:
      - name: agent
        env:
        - name: ROLE_ARN
          value: ""